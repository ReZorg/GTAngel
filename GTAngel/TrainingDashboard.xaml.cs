using System.Windows;

namespace GTAngel;

/// <summary>
/// Training Dashboard — AngelClaw Game World DTE Training Environment.
/// This window hosts the asset browser, training dashboard, evolution cycle, and game runtime tabs.
/// </summary>
public partial class TrainingDashboard : Window
{
    public TrainingDashboard()
    {
        InitializeComponent();
    }
}
